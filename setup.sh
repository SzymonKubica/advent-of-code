# Source this file to set up the runner script
alias runner="runner/target/release/runner"
# sets the root directory of the aoc projects so that the other runner scripts
# can reference it
export AOC_ROOT_DIR=$(pwd)
